apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../infrastructure/nvidia-gpu-operator
# Optional: Add cluster-specific patches here
# patchesStrategicMerge:
#   - helm-release-patch.yaml

# Optional: Add cluster-specific configurations
# patches:
#   - target:
#       kind: HelmRelease
#       name: nvidia-gpu-operator
#     patch: |-
#       - op: replace
#         path: /spec/values/operator/defaultRuntime
#         value: containerd
